Exp3

1️⃣ src/app/app.component.ts


import { Component } from '@angular/core';
import { ReactiveFormsModule, FormGroup, FormControl } from '@angular/forms';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [ReactiveFormsModule],
  templateUrl: './app.html',
  styleUrls: ['./app.css']
})
export class AppComponent {
  title = "Exp_3 Registration";

  registrationForm = new FormGroup({
    username: new FormControl('Admin'),
    password: new FormControl(''),
    cfnpassword: new FormControl('')
  });

  loginRequest() {
    const formValues = this.registrationForm.value;
    if (formValues.password !== formValues.cfnpassword) {
      alert("Passwords do not match!");
      return;
    }
    console.log("Form Submitted:", formValues);
    alert("Registration Successful! Check console for details.");
  }
}



2️⃣ src/app/app.component.html


<div class="container" style="max-width: 500px; margin-top: 50px;">
  <h2>{{ title }}</h2>
  <form [formGroup]="registrationForm" (ngSubmit)="loginRequest()">
    <div class="form-group mb-3">
      <label>Name</label>
      <input formControlName="username" type="text" class="form-control" placeholder="Enter Name" />
    </div>

    <div class="form-group mb-3">
      <label>Password</label>
      <input formControlName="password" type="password" class="form-control" placeholder="Enter Password" />
    </div>

    <div class="form-group mb-3">
      <label>Confirm Password</label>
      <input formControlName="cfnpassword" type="password" class="form-control" placeholder="Confirm Password" />
    </div>

    <button class="btn btn-primary" type="submit">Submit</button>
  </form>
</div>



4️⃣ src/main.ts



import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app/app';

bootstrapApplication(AppComponent)
  .catch(err => console.error(err));

